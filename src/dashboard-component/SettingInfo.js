import React from 'react'
import { connect } from 'react-redux'
import {firebase} from'../firebase/firebase'

const SettingInfo = () => {
    return (
        <div>Setting info </div>
    )
}

export default SettingInfo